[build]
  command = "npm run build"
  publish = "dist"

# Garante que rotas do tipo /alguma-coisa não deem 404
# (não é essencial pra esse site, que é single-page, mas é boa prática)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
